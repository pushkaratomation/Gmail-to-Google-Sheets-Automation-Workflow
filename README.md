# Gmail-to-Google-Sheets-Automation-Workflow
An automated n8n workflow that monitors Gmail for specific incoming emails, extracts key data fields using expressions/regex, and logs them structured into a Google Sheet in real time.
